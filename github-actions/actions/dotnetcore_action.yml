name: Dotnet core Action
on:
  workflow_dispatch:
    input:
      

jobs:
  initialization:
    runs-on: ubuntu-latest
    concurrency:
      group: ci-init-${{github.ref}}
      cancel-in-progress: true
    uses: /jobs/initialization.yml@master
    with:
      versionIncrement: 